{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "XMA 主窗口默认能力。仅启用 Tauri Core 基础能力；更高权限必须按功能单独审查。",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
