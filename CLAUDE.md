# XMA Claude Code 入口

`AGENTS.md` 是 Xiaoyu Management Agent 的最高开发约束，本文件不能覆盖它。

开始任何修改前：

1. 阅读根 `AGENTS.md`；
2. 阅读其中列出的必读架构/开发文档；
3. 根据任务加载 `.claude/skills/` 中的 Skill；公共 Skill 的权威源位于 `.agents/skills/`；
4. 遵守 XMA TypeScript Agent / Rust Native-Security 语言所有权、版本、Windows 工作流、GitHub Safety 和 Gate。

如果本文件、`.claude/` 内容与 `AGENTS.md` 冲突，以 `AGENTS.md` 为准。
