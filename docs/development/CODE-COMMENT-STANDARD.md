# 中文代码注释规范

## 文件头

核心 TypeScript / Rust 文件顶部必须写中文说明，推荐格式：

```text
/**
 * 文件作用：...
 * 关联模块：...
 * 当前实现：...
 * 职责边界：...
 */
```

Rust 使用 `//!` 或块注释。

## 行内注释

必须解释：

- 权限/安全边界；
- 容易误解的协议转换；
- 第三方兼容行为；
- 复杂状态机；
- 不明显的跨平台差异。

不需要解释显而易见的变量赋值。
