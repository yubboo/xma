# 依赖与包管理缓存
node_modules/
.pnpm-store/
.npm/
.yarn/

# 构建产物 / 覆盖率 / 临时缓存
dist/
build/
coverage/
.cache/
.turbo/
tmp/
temp/
*.log
*.tmp
*.bak
*.swp
*.swo

# XMA 用户运行数据、本地依赖/状态与工作区
/runtime/
xma-path/
# 0.1.0 早期遗留目录：新版不再创建，仅保留忽略规则避免旧状态误提交。
.xma/
workspaces/

# Rust 编译缓存
native/target/
**/target/

# Desktop 中间产物统一进入 .cache/desktop；正式发布物统一进入 dist/release。
# 下列旧目录不再作为合法输出，XMA-Sync 会清理；若再次出现应由 Gate/安全检查暴露。

# 本地 Secret / 环境变量
.env
.env.*
!.env.example
**/secrets.json
**/credentials.json
*.pem
*.key
*.pfx
*.p12
*.keystore

# 用户安装包与归档产物（发布走 GitHub Release，不进源码提交）
*.exe
*.msi
*.msix
*.dmg
*.pkg
*.AppImage
*.deb
*.rpm
*.zip
*.7z
*.rar
*.tar
*.tar.gz

# IDE / OS 本地文件
.idea/
.vscode/*.local.json
.DS_Store
Thumbs.db

# Tauri CLI 自身生成的 schema/gen 元数据（Rust target 已统一由 .cache/tauri-target 接管）
apps/desktop/src-tauri/gen/

.xma-package/
